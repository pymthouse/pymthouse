{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "pymthouse: Next dev",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev -- --inspect --experimental-https --experimental-https-key ./certificates/localhost-key.pem --experimental-https-cert ./certificates/localhost.pem",
      "cwd": "${workspaceFolder}"
    }
  ]
}
