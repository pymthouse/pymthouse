{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "pymthouse: Next dev",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev -- --inspect",
      "cwd": "${workspaceFolder}"
    }
  ]
}
