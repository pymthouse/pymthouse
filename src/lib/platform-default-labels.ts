/**
 * User-facing label for the platform-default (Explorer / network-key) app.
 * Keep usage charts, session history, and key source labels aligned on this.
 * Internal bootstrap still uses PLATFORM_DEFAULT_APP_NAME in platform-default-app.ts.
 */
export const PLATFORM_DEFAULT_USAGE_DISPLAY_NAME = "Livepeer Direct";
