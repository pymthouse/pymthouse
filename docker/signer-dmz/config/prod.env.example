# Primary local signer DMZ (repo root: docker compose up).
# Copy to prod.env and adjust; or rely on root .env + defaults.
SIGNER_DMZ_HOST_PORT=8080
SIGNER_DMZ_BIND_HOST=127.0.0.1
SIGNER_DATA_DIR=./data
# OIDC_ISSUER / JWKS_URI default from NEXTAUTH_URL in docker-compose.yml when unset.
